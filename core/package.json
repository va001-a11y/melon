{
  "name": "@melon/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Runtime-agnostic Melon: orchestration, prompts, provider adapters. No Node or DOM-host assumptions, so it runs on a server or in a browser.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
