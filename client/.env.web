# Loaded automatically by `vite --mode web`. Contains no secret — it only
# selects which Melon this build is. Committed on purpose so the web-mode
# scripts work on a fresh clone without anyone having to set a shell
# variable, which differs between cmd, PowerShell and sh.
VITE_MELON_TARGET=browser
